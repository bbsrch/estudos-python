# cálculo do aumento de 15% e do montante total
s1 = float(input('digite seu salário: '))
a1 = s1*0.15
s2 = s1*1.15
print('seu aumento foi de R${:.2f} e seu salário atual é R${:.2f}.'.format(a1, s2))
